{
  "name": "zpu-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "vercel-build": "cd backend && npm run vercel-build",
    "build": "cd backend && npm run build",
    "install:backend": "cd backend && npm install"
  }
}
